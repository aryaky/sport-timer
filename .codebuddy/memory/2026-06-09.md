# 2026-06-09 工作日志

## 修复 macOS Release 应用窗口不显示
- **根本原因**：`beep_player.dart` 引用了已从 `pubspec.yaml` 移除的 `flutter_soloud`，导致 Dart 编译失败，Flutter 引擎无法初始化窗口
- **修复步骤**：
  1. 将 `lib/audio/beep_player.dart` 移到 `_disabled/`（临时禁用音频）
  2. `main.dart` 中注释掉所有 `BeepPlayer` 引用
  3. `flutter analyze` 0 issues
  4. 重新构建后窗口正常显示

## macOS 无边框透明窗口配置
- `macos/Runner/MainFlutterWindow.swift`：在 `awakeFromNib` 中通过代码隐藏标题栏和按钮，设置透明背景
- `macos/Runner/Base.lproj/MainMenu.xib`：保留标准窗口配置确保加载，Swift 中再隐藏 chrome
- `lib/main.dart`：`scaffoldBackgroundColor: Colors.transparent`
- `macos/Runner/AppDelegate.swift`：利用 `FlutterAppDelegate` 继承的 `mainFlutterWindow`（不要重复声明）

## Git 整理
- 更新了 `.gitignore`，排除了构建产物和 IDE 配置
